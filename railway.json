{
    "build": {
      "builder": "dockerfile",
      "buildCommand": "docker build --network=host -t myapp ."
    },
    "deploy": {
      "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT"
    }
  }